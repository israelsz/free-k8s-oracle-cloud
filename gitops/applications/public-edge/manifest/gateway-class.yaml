apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: envoy
spec:
  # Only Envoy Gateway reconciles Gateways that select this class.
  controllerName: gateway.envoyproxy.io/gatewayclass-controller

